# ── Web 框架 ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ── LangChain / LangGraph ────────────────────────────────────────────────────
# langchain-core 是传递依赖，不在此处锁定，由 pip 自动解析兼容版本
# 其余包只限定最低版本，避免手动猜测精确号引发冲突
langchain>=0.2.16
langchain-community>=0.2.16
langchain-openai>=0.1.23,<0.2.0
langgraph>=0.2.28,<0.3.0
langchain-text-splitters>=0.2.4

# ── 向量库 ────────────────────────────────────────────────────────────────────
chromadb>=0.5.5,<0.6.0
langchain-chroma>=0.1.4,<0.2.0   # langchain 官方 Chroma 集成包

# ── 本地嵌入模型（BGE-small）────────────────────────────────────────────────
sentence-transformers==3.0.1
modelscope>=1.15.0            # 国内模型下载源（魔搭社区，阿里云，稳定可达）

# ── 文档解析 ──────────────────────────────────────────────────────────────────
PyPDF2==3.0.1
python-docx==1.1.2

# ── HTTP 客户端 ──────────────────────────────────────────────────────────────
httpx==0.27.0

# ── 配置 & 工具 ──────────────────────────────────────────────────────────────
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.4.1
numpy==1.26.4
